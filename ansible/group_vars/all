# enter your graphite ip (should listen on tcp 2003)
graphite_ip: "192.168.0.1"
graphite_http_port: "8080"

# username used for running tests on all the slave nodes
user: "user"

# file descriptor limit set on the slave nodes
fileno: 500000

# where do you want to install amoc on the master node?
master_install_dir: "/home/{{user}}/amoc_master"

# where do you want to install amoc on a slave node?
slave_install_dir: "/home/{{user}}/amoc"

# erlang node name type: name | sname
node_name_type: "name"
